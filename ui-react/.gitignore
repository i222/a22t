# ------------------------------------------------------------------------------
# Local setup to ignore changes inside mocks/ directory
# This affects only your local Git — useful for test data or local mock configs.
# ------------------------------------------------------------------------------

# Hide changes to already tracked files (run in terminal):
# git ls-files mocks/ | xargs git update-index --assume-unchanged

# List files currently marked as "assume unchanged":
# git ls-files -v | grep '^[a-z]'

# Re-enable tracking for a specific file (if needed):
# git update-index --no-assume-unchanged mocks/example.json

# Ignore newly created files in mocks/ (untracked ones):
# This prevents them from showing up in `git status` or being accidentally committed.
mocks/

# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Windows system files (на всякий случай)
Thumbs.db
ehthumbs.db

# Logs
info/*
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

release-builds/

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

build/Release

# Dependency directories
node_modules/

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Output of 'npm pack'
*.tgz
*.zip

# Yarn Integrity file
.yarn-integrity

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# vuepress build output
.vuepress/dist

# vuepress v2.x temp and cache directory
.temp
.cache

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
